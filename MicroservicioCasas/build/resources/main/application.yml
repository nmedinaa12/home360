server:
  port: 0

springdoc:
  swagger-ui:
    enabled: true
    path: /swagger-ui.html
  api-docs:
    path: /v3/api-docs

spring:
  application:
    name: MicroCasas
  datasource:
    url: jdbc:mysql://localhost:3306/stock
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update